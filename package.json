{
  "name": "confess-game",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:api": "cd api && node -r dotenv/config index.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "install:all": "cd api && npm install && cd ../client && npm install"
  }
}
